[user]
	name = yksz
	email = yksz.dev@gmail.com
[color]
	ui = auto
[push]
	default = simple
[fetch]
	prune = true
[core]
	autocrlf = input
	editor = vim
[alias]
	d = diff
	dr = diff --color-words --word-diff-regex='\\w+|[^[:space:]]'
	dw = diff --word-diff
	lg = log --graph --all --oneline --decorate --tags --pretty='format:%C(yellow)%h %C(cyan)[%an] %C(reset)%s %C(blue)%ar%C(magenta)%d'
	mg = merge --no-ff
	sb = show-branch
	st = status
[http]
#	proxy = http://user:passwd@proxy.example.com:8080
[https]
#	proxy = https://user:passwd@proxy.example.com:8080
